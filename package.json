{
  "name": "new-tab-focus",
  "version": "1.1.2",
  "private": true,
  "license": "MIT",
  "description": "Build tooling for the New Tab Focus Clock browser extension.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:chrome": "node build.mjs chrome",
    "build:firefox": "node build.mjs firefox",
    "test": "node --test tests/*.test.mjs",
    "version": "node version-bump.mjs && git add manifest.json"
  },
  "dependencies": {
    "@ruverse/waves": "^0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
